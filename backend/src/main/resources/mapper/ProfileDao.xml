<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.dao.ProfileDao">

    <resultMap id="BaseResultMap" type="com.example.demo.model.Profile">
        <!--@Table user-->
        <result property="id" column="Id" jdbcType="INTEGER"/>
        <result property="userId" column="userId" jdbcType="INTEGER"/>
        <result property="name" column="name" jdbcType="VARCHAR"/>
        <result property="sex" column="sex" jdbcType="VARCHAR"/>
        <result property="age" column="age" jdbcType="VARCHAR"/>
        <result property="address" column="address" jdbcType="VARCHAR"/>
        <result property="phone" column="phone" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryProfileByUserId" resultMap="BaseResultMap">
        select
        *
        from profile
        where userid = #{userid}
    </select>

    <!--上传信息-->
    <insert id="insertProfile"  keyProperty="id">
        insert into profile(userid,name,sex,age,address,phone)
        values (#{userid}, #{name}, #{sex}, #{age}, #{address}, #{phone})
    </insert>

    <update id="updateProfile" keyProperty="id">
        update profile
        <set>
            name=#{name},
            sex=#{sex},
            age=#{age},
            address=#{address},
            phone=#{phone},
        </set>
        where userid=#{userid}
    </update>



</mapper>

